# Databricks Medallion Architecture — E-Commerce Data Pipeline

## 1. Project Overview

This project implements a full **Bronze → Silver → Gold → Dashboard** medallion architecture data pipeline on **Databricks Community Edition** for an e-commerce company ingesting daily sales data from three CSV sources (customers, orders, products). The pipeline ingests raw data into Delta tables, applies four layers of data quality checks in the Silver layer, produces six business-ready Gold aggregation tables, and surfaces insights through a Databricks SQL dashboard. The entire pipeline was built with AI-assisted development using Cursor, with full prompt history and design documentation included in the repository.

---

## 2. Architecture

```
data/ (local CSV files — generated by sample data script)
    customers.csv  |  orders.csv  |  products.csv
                          │
                    Upload to DBFS
                          │
               /FileStore/tables/*.csv
                          │
                          ▼
            ┌─────────────────────────┐
            │      BRONZE LAYER       │  Raw ingest, no transformations
            │  bronze_customers       │  + ingestion_timestamp
            │  bronze_orders          │  + source_file_name
            │  bronze_products        │
            └────────────┬────────────┘
                         │  Reads Bronze Delta tables only
                         ▼
            ┌─────────────────────────┐
            │      SILVER LAYER       │  Data quality checks
            │  silver_customers       │  + quality_check_result
            │  silver_orders          │  (PASS / FAIL - reason)
            │  silver_products        │  Bad rows flagged, never deleted
            └────────────┬────────────┘
                         │  Reads Silver Delta tables (PASS rows only)
                         ▼
            ┌─────────────────────────┐
            │       GOLD LAYER        │  Business aggregations
            │  gold_sales_by_product  │
            │  gold_revenue_by_       │
            │    customer             │
            │  gold_daily_trends      │
            │  gold_weekly_trends     │
            │  gold_customer_         │
            │    segmentation         │
            │  gold_customer_         │
            │    segment_detail       │
            └────────────┬────────────┘
                         │
                         ▼
            ┌─────────────────────────┐
            │   DATABRICKS SQL        │  5 dashboard queries
            │   DASHBOARD             │  Bar / Histogram / Pie /
            │                         │  Line / Stacked Bar charts
            └─────────────────────────┘
```

---

## 3. Repository Structure

```
C1 Assessment/
│
├── README.md                        ← This file — setup and project guide
├── candidate-info.md                ← Candidate details and tool declaration
├── tool-workflow.md                 ← Part A: AI workflow methodology (20%)
├── requirements-analysis.md        ← Business requirements and acceptance criteria
├── design-notes.md                 ← Architecture and design decisions
├── data-model.md                   ← Full schema across all four layers
├── data-quality-strategy.md        ← Strategy for all 4 Silver quality checks
├── debugging-notes.md              ← Bugs encountered and how they were resolved
├── reflection.md                   ← What was learned; what would be done differently
├── final-ai-usage-summary.md       ← Summary of AI usage across the full lifecycle
│
├── src/
│   ├── data_generation/
│   │   ├── generate_sample_data.py  ← Generates 3 CSVs with intentional quality issues
│   │   └── DATA_GENERATION_NOTES.md ← Notes on data design and quality issue choices
│   │
│   ├── bronze/
│   │   ├── 01_ingest_customers.py   ← Ingests customers.csv → bronze_customers Delta table
│   │   ├── 02_ingest_orders.py      ← Ingests orders.csv → bronze_orders Delta table
│   │   ├── 03_ingest_products.py    ← Ingests products.csv → bronze_products Delta table
│   │   └── ingest_all.py            ← Orchestrator: runs all 3 Bronze scripts in sequence
│   │
│   ├── silver/
│   │   ├── 01_quality_completeness.py        ← Flags NULL emails, customer_ids, product_ids
│   │   ├── 02_quality_uniqueness.py          ← Flags duplicate PKs using ROW_NUMBER()
│   │   ├── 03_quality_type_validation.py     ← Flags invalid dates, negative numerics
│   │   ├── 04_quality_referential_integrity.py ← Flags orphan FK rows via LEFT JOIN
│   │   ├── 05_quality_business_logic.py      ← Flags amount mismatches, payment date rules
│   │   └── create_silver_tables.py           ← Orchestrator: merges all checks into final Silver tables
│   │
│   ├── gold/
│   │   ├── 01_sales_by_product.py    ← Revenue and order count aggregated per product
│   │   ├── 02_revenue_by_customer.py ← Revenue and order count aggregated per customer
│   │   ├── 03_daily_weekly_trends.py ← Daily and weekly revenue trends (Completed orders)
│   │   ├── 04_customer_segmentation.py ← Behavioural segmentation using PERCENT_RANK()
│   │   └── create_gold_tables.py     ← Orchestrator: runs all 4 Gold scripts in sequence
│   │
│   └── dashboard/
│       ├── dashboard_queries.sql     ← 5 Databricks SQL queries powering the dashboard
│       └── DASHBOARD_GUIDE.md       ← Step-by-step guide to set up the Databricks SQL Dashboard
│
├── data/
│   ├── customers.csv                ← 10,010 rows (includes 50 NULL emails + 10 duplicates)
│   ├── orders.csv                   ← 100,020 rows (includes 400 planted quality issues)
│   └── products.csv                 ← 500 rows (clean — no intentional issues)
│
├── database/
│   ├── schema.sql                   ← Reference CREATE TABLE definitions for all 12 Delta tables
│   ├── seed-data-notes.md           ← Notes on the synthetic data design choices
│   └── setup-notes.md              ← Notes on Databricks environment setup
│
└── ai-prompts/
    ├── documentation.md             ← Prompts for requirements, design docs, .cursorrules
    ├── data-generation.md           ← Prompts for generate_sample_data.py
    ├── bronze-layer.md              ← Prompts for Bronze ingestion scripts
    ├── silver-layer.md              ← Prompts for Silver quality check scripts
    ├── gold-layer.md                ← Prompts for Gold aggregation scripts
    ├── dashboard.md                 ← Prompts for dashboard queries
    └── debugging.md                 ← Prompts used during debugging sessions
```

---

## 4. Prerequisites

| Requirement | Details |
|:---|:---|
| Databricks Community Edition | Free account at [community.cloud.databricks.com](https://community.cloud.databricks.com) |
| Python 3.8+ | For running the local data generation script |
| Python libraries | `faker`, `pandas`, `numpy` (installed via pip) |
| Git | For cloning the repository |

---

## 5. Setup Instructions

### Step 1 — Clone the Repository

```bash
git clone <your-repo-url>
cd C1-Assessment
```

### Step 2 — Generate Sample Data Locally

Run the data generation script on your local machine. This creates all three CSV files in the `data/` folder with realistic data and intentional quality issues pre-planted.

```bash
# Install required libraries (use a virtual environment if preferred)
pip install faker pandas numpy

# Generate the CSVs
python src/data_generation/generate_sample_data.py
```

**Expected output:**
```
customers.csv — 10,010 rows | 50 NULL emails | 10 duplicate customer_ids
orders.csv    — 100,020 rows | 100 NULL customer_ids | 200 NULL product_ids | 50 orphans | 30 orphans | 20 duplicates
products.csv  — 500 rows | 0 issues
```

### Step 3 — Upload CSVs to DBFS

1. Log in to **Databricks Community Edition**
2. Navigate to **Data → Add Data → Upload File**
3. Upload `data/customers.csv`, `data/orders.csv`, `data/products.csv`
4. Confirm each file lands at `/FileStore/tables/`:
   - `/FileStore/tables/customers.csv`
   - `/FileStore/tables/orders.csv`
   - `/FileStore/tables/products.csv`

### Step 4 — Create a Databricks Cluster

1. Go to **Compute → Create Cluster**
2. Set **Databricks Runtime**: `13.3 LTS` or above (includes Delta Lake)
3. Use the default **Single Node** configuration (Community Edition)
4. Click **Create Cluster** and wait for it to start

### Step 5 — Run the Bronze Layer

1. Open a new **Databricks Notebook**
2. Copy the contents of `src/bronze/ingest_all.py` into a cell (or upload as a script)
3. Attach the notebook to your cluster and **Run All**
4. Verify the output shows:

```
BRONZE INGESTION PIPELINE — MASTER SUMMARY
Overall Status: ALL SUCCEEDED
  Ingest Customers    SUCCESS
  Ingest Products     SUCCESS
  Ingest Orders       SUCCESS
```

> The script runs in order: customers → products → orders (products before orders because orders references product_ids).

### Step 6 — Run the Silver Layer

1. Open a new notebook and copy `src/silver/create_silver_tables.py`
2. Run all cells
3. Verify the quality report shows expected pass/fail counts:

```
silver_customers : ~10,010 total | ~9,950 passed | ~60 failed
silver_orders    : ~100,020 total | ~99,620 passed | ~400 failed
silver_products  :     500 total |    500 passed |   0 failed
```

### Step 7 — Run the Gold Layer

1. Open a new notebook and copy `src/gold/create_gold_tables.py`
2. Run all cells
3. Verify all 6 Gold Delta tables are created:

```
GOLD LAYER — FINAL TABLE INVENTORY
✓ gold_sales_by_product
✓ gold_revenue_by_customer
✓ gold_daily_trends
✓ gold_weekly_trends
✓ gold_customer_segmentation
✓ gold_customer_segment_detail
```

### Step 8 — Set Up the Dashboard

1. Go to **Databricks SQL → Dashboards → Create Dashboard**
2. Register Gold tables as SQL tables (run in a SQL query editor):

```sql
CREATE TABLE gold_sales_by_product
  USING DELTA LOCATION '/FileStore/delta/gold/gold_sales_by_product';

CREATE TABLE gold_revenue_by_customer
  USING DELTA LOCATION '/FileStore/delta/gold/gold_revenue_by_customer';

CREATE TABLE gold_customer_segmentation
  USING DELTA LOCATION '/FileStore/delta/gold/gold_customer_segmentation';

CREATE TABLE gold_weekly_trends
  USING DELTA LOCATION '/FileStore/delta/gold/gold_weekly_trends';

CREATE TABLE silver_orders
  USING DELTA LOCATION '/FileStore/delta/silver/silver_orders';
```

3. Copy and run each query from `src/dashboard/dashboard_queries.sql`
4. Add visualizations:
   - Query 1 → Bar chart (Top 10 products by revenue)
   - Query 2 → Bar/Histogram (Customer revenue distribution)
   - Query 3 → Pie chart (Customer segmentation)
   - Query 4 → Line chart (Weekly revenue trend)
5. See `src/dashboard/DASHBOARD_GUIDE.md` for detailed step-by-step instructions with screenshots guidance.

---

## 6. Data Quality Summary

The Silver layer catches all intentionally planted quality issues without deleting any rows. Every row is preserved and flagged with a `quality_check_result` column.

| Issue Type | Count | Silver Check |
|:---|:---:|:---|
| NULL emails in customers | 50 | Completeness check |
| Duplicate `customer_id` in customers | 10 | Uniqueness check |
| NULL `customer_id` in orders | 100 | Completeness check |
| NULL `product_id` in orders | 200 | Completeness check |
| Orphan `customer_id` in orders | 50 | Referential integrity check |
| Orphan `product_id` in orders | 30 | Referential integrity check |
| Duplicate `order_id` in orders | 20 | Uniqueness check |
| **Total flagged rows** | **~460** | *(some rows fail multiple checks)* |

Gold aggregations use **PASS rows only**, ensuring quality issues never contaminate business reporting.

---

## 7. AI Tool Usage

This project was built end-to-end using **Cursor** as the primary AI assistant. AI was used throughout the full development lifecycle — requirements analysis, pipeline design, code generation (PySpark, SQL, Python), data quality strategy, documentation, and reflection. A persistent context file (`.cursorrules`) was maintained throughout to give the AI full project awareness across sessions. The complete AI prompt history, organized by pipeline stage, is available in the `ai-prompts/` folder. The full methodology, including what was accepted, modified, and rejected from AI suggestions, is documented in `tool-workflow.md`.

---

## Known Limitations

**Silver layer row count inflation:**
The final silver_customers table contains 10,150 rows instead of the
source 10,010. Similarly silver_orders shows 100,620 vs 100,020.
This occurs because create_silver_tables.py LEFT JOINs multiple quality
check output tables. Rows failing multiple checks appear in more than
one check table and get duplicated in the join. PASS row counts are
correct and Gold layer is unaffected since it filters to PASS rows only.
In production this would be fixed using a single-pass flagging approach
rather than joining separate check outputs.

**Dashboard SQL queries:**
Dashboard tiles are documented in src/dashboard/dashboard_queries.sql
and src/dashboard/DASHBOARD_GUIDE.md but require a live Databricks SQL
environment to render visually. The queries are verified correct against
the Gold table schemas.

**Gold file extensions:**
Gold scripts use .py extension instead of .sql as shown in the brief
template. This is intentional — .sql files cannot write Delta tables.
PySpark is required for Delta Lake writes.

**Local vs Databricks paths:**
Source files use local relative paths (data/, output/delta/) for local
testing. Original DBFS paths (/FileStore/) are preserved in
dashboard_queries.sql and DASHBOARD_GUIDE.md for Databricks deployment.

---

## 8. Project Status

| Component | Status |
|:---|:---:|
| Sample data generator (`generate_sample_data.py`) | ✅ Complete |
| Data CSVs with intentional quality issues | ✅ Complete |
| Bronze layer — all 3 sources ingested | ✅ Complete |
| Silver — Completeness check | ✅ Complete |
| Silver — Uniqueness check | ✅ Complete |
| Silver — Referential integrity check | ✅ Complete |
| Silver — Type validation check | ✅ Complete |
| Silver — Business logic check *(bonus)* | ✅ Complete |
| Gold — Sales by product | ✅ Complete |
| Gold — Revenue by customer | ✅ Complete |
| Gold — Customer segmentation | ✅ Complete |
| Gold — Daily/weekly trends *(bonus)* | ✅ Complete |
| Dashboard SQL queries (5 queries) | ✅ Complete |
| Database schema reference (`schema.sql`) | ✅ Complete |
| AI prompt history (`ai-prompts/`) | ✅ Complete |
| Requirements analysis, design, data model | ✅ Complete |
| Tool workflow document (`tool-workflow.md`) | ✅ Complete |
| README setup instructions | ✅ Complete |
| Reflection and final AI usage summary | ✅ Complete |
